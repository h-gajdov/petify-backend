# Copy to .env for Docker Compose or .env.properties for Spring Boot.
# If using an existing PostgreSQL volume, use its existing credentials.
DB_LOCAL_USERNAME=user
DB_LOCAL_PASSWORD=password
DB_LOCAL_NAME=petify_local
